<header>
  <nav class="navbar navbar-expand-lg bg-dark" data-bs-theme="dark">
    <div class="container-fluid text-light">
      <a class="navbar-brand" href="{{path('index')}}">IMDB-Like</a>
      
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
        aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
          <li class="nav-item align-self-end align-md-auto">
            <a class="nav-link" href="{{path('films')}}"><i class="bi bi-film fs-3 pe-1"></i>Films</a>
          </li>

          {% if app.request.pathinfo|slice(1)|lower == 'films' %}
          <li class="nav-item align-self-end align-md-auto">
            <a class="nav-link btn" onclick="HideAndShowMainSection()" id="btnSeekMovies" 
              data-bs-toggle="modal" data-bs-target="#popupMoviesForm"><i class="bi bi-search fs-3 pe-1"></i>Recherche</a>
          </li>
          {% endif %}

        </ul>

      </div>

    </div>
  </nav>
</header>